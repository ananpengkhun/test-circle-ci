version: 2
jobs:
  build:
    docker:
      - image: circleci/android:api-28-alpha
    steps:
      - checkout
      - run: ./gradlew clean
      - run: ./gradlew build
      - run: ./gradlew assembleDebug
      - run: hockey(
                   api_token: "4bac0a6e9f054038bbb899acf442d08a",
                   ipa: 'app/build/outputs/apk/debug/app-debug.apk',
                   notes_type: "1",
                   notes: changelog,
                 )